진동수 구하는 알고리즘 생각
화면에 여유 공간이 없어 넣지는 않았지만 진동수를 구하는 알고리즘을 생각해보면
for break으로 0번 버퍼의 값과 같은 값을 같는 버퍼 인덱스를 찾는다.

if(!button0LV) {
    ((해당 버퍼 인덱스)+1)/77.0
}
else {
((해당 버퍼 인덱스)+1)*delayTerm
}
으로 주기 구하고, 역수 취하면 됨

좀더 일반화 하면 77.0 대신 (F_CPU/PRESCALE/13.0) 으로(단위 1/k이니 ms)